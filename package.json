{
  "name": "ob-discrepancy-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "OneBusiness — дашборд расхождений ArmSoft / TaxService / выгрузка Артёма",
  "scripts": {
    "build": "vite build",
    "start": "vite --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "vite": "^6.0.0"
  }
}
